# Scheduled task templates

Place **sanitized** `.xml` or `.template.xml` scheduled task definitions here.

- No real user paths (use `%USERPROFILE%` or a documented variable).
- Run `.\scripts\03_tasks_import.ps1` to import (as Administrator).

See `config\templates\schtasks-export.example.txt` for export/sanitize steps.
